node_modules
dist
.env
*.log
.DS_Store
/runs/
.superpowers/
.next/
/data/
tsconfig.tsbuildinfo
.agents/
skills-lock.json
.vercel
.env*

# demo-video working dir (raw takes, mp3s, intermediate + final renders)
# root-anchored so it does NOT match scripts/video/ (the pipeline source)
/video/
